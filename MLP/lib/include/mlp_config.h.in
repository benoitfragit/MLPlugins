#ifndef MLP_CONFIG_H
#define MLP_CONFIG_H

#include "mlp_types.h"

#define NETWORK_XSD_FILE  "${CMAKE_INSTALL_PREFIX}/schemas/network.xsd"
#define INIT_XSD_FILE     "${CMAKE_INSTALL_PREFIX}/schemas/init.xsd"
#define SETTINGS_XSD_FILE "${CMAKE_INSTALL_PREFIX}/schemas/settings.xsd"
#define TRAINING_XSD_FILE "${CMAKE_INSTALL_PREFIX}/schemas/training.xsd"
#define BRAIN_ENCODING    "ISO-8859-1"

static MetaData mlpInfos = {"${BRAIN_NAME}",
                            "${BRAIN_AUTHOR}",
                            "${BRAIN_VERSION}",
                            "${BRAIN_DESCRITION}"} ;

#endif /* MLP_CONFIG_H */
