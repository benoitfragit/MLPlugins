#!/bin/bash

CMAKE_NB_CORES=3
CMAKE_ENABLE_DOC=1
CMAKE_ENABLE_INSTALL=1

LIBBRAIN_ROOT_DIR=$(pwd)
LIBBRAIN_SOURCE_DIR=$LIBBRAIN_ROOT_DIR
LIBBRAIN_BUILD_TYPE=release
LIBBRAIN_VERSION_MAJOR=1
LIBBRAIN_VERSION_MINOR=0
LIBBRAIN_BUILD_DIR=$LIBBRAIN_ROOT_DIR/build/$LIBBRAIN_BUILD_TYPE/libbrain
LIBBRAIN_INSTALL_DIR=$LIBBRAIN_ROOT_DIR/install/$LIBBRAIN_BUILD_TYPE/libbrain-$LIBBRAIN_VERSION_MAJOR.$LIBBRAIN_VERSION_MINOR
LIBBRAIN_MODULE_PATH=$LIBBRAIN_ROOT_DIR/scripts/cmake/Modules
LIBBRAIN_ARCHIVE_TYPE=TBZ2

echo ""
echo "LIBBRAIN_ROOT_DIR    << $LIBBRAIN_ROOT_DIR"
echo "LIBBRAIN_SOURCE_DIR  << $LIBBRAIN_SOURCE_DIR"
echo "LIBBRAIN_BUILD_DIR   << $LIBBRAIN_BUILD_DIR"
echo "LIBBRAIN_INSTALL_DIR << $LIBBRAIN_INSTALL_DIR"
echo "LIBBRAIN_MODULE_PATH << $LIBBRAIN_MODULE_PATH"
echo ""

if [ ! -d "$LIBBRAIN_BUILD_DIR" ]; then
    mkdir -p $LIBBRAIN_BUILD_DIR
fi

if [ ! -d "$LIBBRAIN_INSTALL_DIR" ]; then
    mkdir -p $LIBBRAIN_INSTALL_DIR
fi

cd $LIBBRAIN_BUILD_DIR

clear;
clear;

cmake -DCMAKE_BUILD_TYPE=$LIBBRAIN_BUILD_TYPE           \
      -DLIBBRAIN_VERSION_MAJOR=$LIBBRAIN_VERSION_MAJOR  \
      -DLIBBRAIN_VERSION_MINOR=$LIBBRAIN_VERSION_MINOR  \
      -DCMAKE_INSTALL_PREFIX:PATH=$LIBBRAIN_INSTALL_DIR \
      -DCMAKE_MODULE_PATH=$LIBBRAIN_MODULE_PATH         \
      -DCPACK_SOURCE_GENERATOR=$LIBBRAIN_ARCHIVE_TYPE   \
      $LIBBRAIN_SOURCE_DIR

make clean
make -j$CMAKE_NB_CORES

if [ $CMAKE_ENABLE_DOC -eq 1 ]; then
    make doc
fi

if [ $CMAKE_ENABLE_INSTALL -eq 1 ]; then
    make install
fi

exit 0
